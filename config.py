# =============================================================================
# config.py — Конфигурация бота (ЛЁГКАЯ ВЕРСИЯ для bothost.ru)
# =============================================================================
# Без torch, без CLIP — только OCR + цветовой анализ + хеширование
# =============================================================================

import os
from pathlib import Path

try:
    from dotenv import load_dotenv
    load_dotenv()
except ImportError:
    pass

# ---------------------------------------------------------------------------
# Токен Discord-бота
# ---------------------------------------------------------------------------
TOKEN = os.getenv("DISCORD_TOKEN", "YOUR_BOT_TOKEN_HERE")

# ---------------------------------------------------------------------------
# ID сервера (0 = все серверы)
# ---------------------------------------------------------------------------
GUILD_ID = int(os.getenv("GUILD_ID", "0"))

# ---------------------------------------------------------------------------
# ID канала для логов (0 = не отправлять, настраивается через !setlogchannel)
# ---------------------------------------------------------------------------
LOG_CHANNEL_ID = int(os.getenv("LOG_CHANNEL_ID", "0"))

# ---------------------------------------------------------------------------
# Наказания
# ---------------------------------------------------------------------------
MAX_WARNINGS = 3
MUTE_DURATION = int(os.getenv("MUTE_DURATION", "3600"))  # секунды

# ---------------------------------------------------------------------------
# Детекция
# ---------------------------------------------------------------------------
CONFIDENCE_THRESHOLD = float(os.getenv("CONFIDENCE_THRESHOLD", "0.55"))

# ---------------------------------------------------------------------------
# Пути
# ---------------------------------------------------------------------------
BASE_DIR = Path(__file__).parent
DB_PATH = str(BASE_DIR / "warnings.db")
LOGS_DIR = str(BASE_DIR / "logs")
REVIEW_DIR = str(BASE_DIR / "logs" / "review")
LOG_FILE = str(BASE_DIR / "logs" / "violations.log")
HASH_DB_PATH = str(BASE_DIR / "known_spam_hashes.json")

# ---------------------------------------------------------------------------
# Форматы изображений
# ---------------------------------------------------------------------------
SUPPORTED_FORMATS = {".png", ".jpg", ".jpeg", ".gif", ".webp", ".bmp"}

# ---------------------------------------------------------------------------
# Сообщения
# ---------------------------------------------------------------------------
MSG_WARNING = (
    "⚠️ {mention}, ваше сообщение удалено — обнаружена реклама казино/гемблинга.\n"
    "Предупреждение: **{warnings}/{max_warnings}**\n"
    "Уверенность: {confidence:.0%} | Метод: {method}"
)
MSG_MUTED = (
    "🔇 {mention} получил мут на {duration} мин. за спам-рассылку.\n"
    "Причина: {warnings} предупреждений за рекламу казино."
)
MSG_REVIEW = (
    "🔍 Подозрительное изображение от {mention}\n"
    "Уверенность: {confidence:.0%} (ниже порога — не удалено)"
)
